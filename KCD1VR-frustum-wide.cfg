; KCD1VR peripheral-culling diagnostic. The OpenXR per-eye projection remains
; headset-native; this only widens CryEngine's center camera used for visibility
; and LOD decisions.
; Add after either culling profile, for example:
; +exec KCD1VR.cfg +exec KCD1VR-culling-balanced.cfg +exec KCD1VR-frustum-wide.cfg

cl_fov = 100
