; KCD1VR isolation profile. Disable only CryEngine's master object-visibility
; check. If this reproduces both the fix and the full performance loss, this is
; the expensive switch in the broader profiles.
; Launch with:
; +exec KCD1VR.cfg +exec KCD1VR-check-occlusion-off.cfg

e_CheckOcclusion = 0
